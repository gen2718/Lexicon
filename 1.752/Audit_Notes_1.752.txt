Pre-patch audit · 2026-09-16
A: Current labels to correct: meta description 20; static family selector 43; public title, edition, flow applicability, guide, header 1.751; current release date; Calibration download filename.
B: Historical records to retain: entry baselineKind, original 20/43 stages, versionNotes16/17, notes175, qa17/qa175, flowExtension including its 1.751 date and QA, 1.75 retained guide, historical research headings. DB.versionNotes[0] will explicitly label inherited 20 vs current 87.
C: Retain schemaVersion 1.75.0, export schema lexicon-calibration-1.75, and record.version 1.75 as content-baseline compatibility fields. Add explanatory app/schema/content version fields to export envelope; no change to records or schema identifier.
D: renderNavV15 has zero callers (one definition only). render() calls renderBefore175(), which calls renderNav(); search/filter events also call renderNav(). Keep legacy functions. Change only obsolete navigation denominator to E.length as a regression precaution. Old guide renderers and refsBlockV16 have no current incoming call path; their edition-specific descriptions remain historical source code.
Current renderGuide calls renderGuideBeforeFlow (1.75 retained guide), not 1.5/1.6/1.7 guide renderers. renderDiagBefore175's 43には数えない is replaced by current renderDiag before display; make the underlying phrase 87 as a minimal fallback correction. headerBefore175 is a retained internal step whose public edition is replaced by headerBeforeFlow then header.
No footer version exists. Current footer count is already 87. Screen-reader count uses E.length. No separate print count/version surface. Current registry/network count is 87. Calibration has no import handler; preserve old envelope fields and record structure for downstream consumers, whose external parsers are not available for testing.
All matches, including current correct values and meaningful historical values, are recorded in the accompanying inventory. A=current correction, B=history/content retention, C=schema compatibility, D=legacy source.
